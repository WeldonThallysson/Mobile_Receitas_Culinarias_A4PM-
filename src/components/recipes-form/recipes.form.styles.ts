import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  dialog: {
    borderRadius: 24,
  },

  content: {
    gap: 8,
    paddingVertical: 8,
  },

  row: {
    flexDirection: 'row',
    gap: 5,
  },

  flex: {
    flex: 1,
  },
});